# HideIn

Inject hidden text into PDFs.

## Structure

- `frontend/` — SvelteKit 5 static site
- `backend/` — Java 25 Javalin API

## Dev

```bash
# frontend
cd frontend && pnpm install && pnpm dev

# backend
cd backend && ./gradlew run
```

## License

MIT
